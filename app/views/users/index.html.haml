%h1 Users#index
%p Find me in app/views/users/index.html.erb
- @users.each do |user|
  - if current_user.id == user.id
    = user.email
    = link_to 'Edit my Profile' , edit_user_path(current_user)
    %br/
    = link_to 'Home' , root_path
